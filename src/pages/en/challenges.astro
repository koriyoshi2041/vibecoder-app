---
import Layout from '../../layouts/Layout.astro'
import ChallengeFilter from '../../components/ChallengeFilter'
import { t } from '../../i18n'

const locale = 'en' as const
---

<Layout title="Taste Challenges — VibeCoder Learn" locale={locale}>
  <section class="py-4 sm:py-8">
    <p class="font-mono text-sm text-rust mb-2 tracking-wide">{t('challenges.label', locale)}</p>
    <h1 class="text-2xl sm:text-3xl font-bold text-charcoal mb-2">{t('challenges.title', locale)}</h1>
    <p class="text-charcoal-light mb-1">{t('challenges.subtitle', locale)}</p>
    <p class="text-sm text-warm-gray mb-8">{t('challenges.meta', locale)}</p>

    <ChallengeFilter client:load locale="en" />
  </section>
</Layout>
